# Open Notebook - pip requirements (generated from pyproject.toml)
# Python 3.11-3.12 required.
# For the canonical source of truth see pyproject.toml / uv.lock.

# Framework
fastapi>=0.104.0
uvicorn>=0.24.0
uvloop>=0.21.0        
pydantic>=2.9.2
python-dotenv>=1.0.1
httpx[socks]>=0.27.0
loguru>=0.7.2

# LangChain / LangGraph
langchain>=1.2.0
langchain-community>=0.4.1
langgraph>=1.0.5
langgraph-checkpoint-sqlite>=3.0.1
tiktoken>=0.12.0

# LLM provider adapters
langchain-openai>=1.1.6
langchain-anthropic>=1.3.0
langchain-ollama>=1.0.1
langchain-google-genai>=4.1.2
langchain-groq>=1.1.1
langchain_mistralai>=1.1.1
langchain_deepseek>=1.0.0
langchain-huggingface>=0.0.1

# Multi-provider abstraction + prompt templating
esperanto>=2.19.7,<3
ai-prompter>=0.3,<1

# Content extraction (PDF, URLs, audio, video, etc.)
content-core>=1.14.1,<2

# Podcast generation (outline + transcript + TTS)
podcast-creator>=0.12.0,<1

# Database and async jobs
surrealdb>=1.0.4
surreal-commands>=1.3.1,<2

# Search
opensearch-py>=2.4.0

# Pin requests to satisfy arxiv~=2.32.0 constraint
requests~=2.32.0

# Misc utilities
numpy>=2.4.1
tomli>=2.0.2
pycountry>=26.2.16
babel>=2.18.0

# ---- Development / testing (optional) ----
# pytest>=8.0.0
# pytest-asyncio>=1.2.0
# ruff>=0.14.13
# mypy>=1.11.1
# pre-commit>=4.1.0
# ipykernel>=6.29.5
# ipywidgets>=8.1.5
# types-requests>=2.32.4.20250913

# ---- Audio: transcription & diarization ----
# Whisper + Pyannote run in a dedicated NOVA-Researcher tool server
# (see NOVA-Researcher/whisper_server.py, port 4805). Open-Notebook only
# proxies HTTP requests to it; no extra Python deps are required here.
# Configure WHISPER_API_URL (default http://localhost:4805) if needed.
